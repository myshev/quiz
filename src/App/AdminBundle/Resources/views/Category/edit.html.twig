{% extends 'AdminBundle:CRUD:edit.html.twig' %}

{% block breadcrumbs %}
    {% if isNew %}
        {{ app.breadcrumbs.prepend('Добавление категории') }}
    {% else %}
        {{ app.breadcrumbs.prepend('Редактирование категории') }}
    {% endif %}
    {{ app.breadcrumbs.prepend('Категории', path('admin_category_list')) }}
    {{ parent() }}
{% endblock %}

{% block panel_title %}
    {% if isNew %}
        {{ 'Добавление категории' }}
    {% else %}
        {{ 'Редактирование категории' }}
    {% endif %}
{% endblock %}